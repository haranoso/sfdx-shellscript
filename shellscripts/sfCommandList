
echo "すべてプロジェクトのカレントディレクトリで実行される前提です。force-app等があるディレクトリ階層。"
echo "--"
echo "getAPINames {sObjectName} : カスタムオブジェクトのAPI名を指定すると、項目のAPI名を表示する。"
echo "--"
echo "llf : 一番最後に保存されているログファイルを表示する。"
echo "--"
echo "retrieve : retrieveを実行する。"
echo "--"
echo "retrieveSlack : retrieveを実行し、Slackへ通知する。"
echo "--"
echo "sfexec {apexfile path} : apexコードを実行する。"
echo "--"
echo "sft : log:tailコマンドを実行する。※終了するまで表示し続ける"
echo "--"
echo "sftd : log:tailコマンドかつDEBUG行を表示する。※終了するまで表示し続ける"
echo "--"
echo "sftf : log:tailコマンドかつFATAL行を表示する。※終了するまで表示し続ける"
echo "--"
echo "soql {soql文} : {SOQL文}に記載されたSOQLを実行します。このコマンドはSELECT　ONLY"
echo "--"
echo "th $1 : apex Testを　human形式表示かつCoverageなしで実行する。"
echo "--"
echo "thc $1 : apex Testを　human形式表示かつCoverageありで実行する。"
echo "--"
echo "tt $1 : apex Testを　Tap形式表示かつCoverageなしで実行する。"
echo "--"
echo "ttslack $1 : ttの実行結果をSlackに通知する。"
echo "--"
echo "ttc $1 : apex Testを　Tap形式表示かつCoverageありで実行する。"
echo "--"
echo "ttcRecursion $1 : ttcを指定したクラス名で検索して再帰的に実行する。"
echo "--"
echo "tthRecursion $1 : tthを指定したクラス名で検索して再帰的に実行する。"
